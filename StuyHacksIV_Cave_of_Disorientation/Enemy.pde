public class Enemy{
  int health;
  int maxHealth;
  Bar healthBar;
  public Enemy(/*int health, int maxHealth*/){
    //healthBar = new Bar(new PVector(hitBox.TR.x,hitBox.TR.y - 50),new PVector(60,20),maxHealth);
  }
}